#!/bin/bash

# Memora - 从 icon.kitchen 导入图标的详细步骤

cat << 'EOF'

📋 从 icon.kitchen 导入图标到 Xcode 的步骤：

════════════════════════════════════════════════════════════════

1️⃣ 下载并解压图标
   - 从 icon.kitchen 下载的 zip 文件
   - 双击解压到文件夹

2️⃣ 打开 Xcode 项目
   - 打开 Memora.xcodeproj
   - 在左侧导航栏找到 Assets.xcassets
   - 点击展开，找到 AppIcon

3️⃣ 导入图标文件

   方法 A - 自动导入（推荐）：
   ────────────────────────────────
   - 选中 AppIcon
   - 将解压后的**整个文件夹**拖入 Xcode 窗口
   - Xcode 会自动识别并放置到正确位置

   方法 B - 手动导入：
   ────────────────────────────────
   - 在 AppIcon 界面，你会看到很多空白方框
   - 每个方框下面标注了尺寸（如 16x16, 32x32 等）
   - 从解压的文件夹中找到对应尺寸的图片
   - 将图片拖入对应的方框

   文件名对应关系：
   icon_16x16.png      → 16pt Mac 1x
   icon_16x16@2x.png   → 16pt Mac 2x
   icon_32x32.png      → 32pt Mac 1x
   icon_32x32@2x.png   → 32pt Mac 2x
   （以此类推...）

4️⃣ 验证导入
   - 所有方框都应该显示图标预览
   - 如果有空白，说明该尺寸缺失

5️⃣ 清理并重新编译
   在 Xcode 菜单栏：
   - Product → Clean Build Folder (⇧⌘K)
   - Product → Run (⌘R)

6️⃣ 查看效果
   - 应用运行后，在 Dock 栏查看图标
   - 在启动台(Launchpad)查看图标

════════════════════════════════════════════════════════════════

💡 小贴士：

✅ icon.kitchen 生成的文件已经包含所有需要的尺寸
✅ 如果使用方法A自动导入，Xcode 会智能匹配文件名
✅ macOS 图标使用圆角矩形，不需要手动裁剪
✅ 支持暗黑模式，图标会自动适配

🔧 常见问题：

Q: 导入后看不到图标？
A: 清理项目 (Clean Build Folder) 然后重新编译

Q: 图标显示不正确？
A: 检查 Assets.xcassets/AppIcon.appiconset/Contents.json 文件

Q: 需要所有尺寸都填充吗？
A: 建议填充所有尺寸以获得最佳显示效果

════════════════════════════════════════════════════════════════

EOF
